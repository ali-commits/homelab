[Resolve]
# Disable systemd-resolved DNS stub listener to free port 53 for AdGuard
DNSStubListener=no
# Use AdGuard as DNS server (currently on port 5353)
DNS=127.0.0.1:5353
# Fallback DNS servers
FallbackDNS=1.1.1.1 8.8.8.8
