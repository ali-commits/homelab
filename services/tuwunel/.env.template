# Copy to .env and fill in; .env is gitignored and chmod 600.
# PERMANENT: baked into every user ID, cannot be changed without a database wipe.
MATRIX_SERVER_NAME=alimunee.com
# Shared invite token: registration is impossible without it. Rotate after onboarding.
REGISTRATION_TOKEN=
